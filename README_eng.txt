The mod is a set of settings for commanders configurable via json:
/Lang/jscommandersmodule_settings.json

List of options:
- see jscommandersmodule_settings.json: the list contains several options, each with a description of its effect and compatibility with WoG options
- to disable an option, change the "value" key from true to false, and vice versa
